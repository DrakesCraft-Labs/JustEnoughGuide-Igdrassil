enabled: true

groups:
  group1:
    enabled: false
    tier: 1
    id: custom-group # [a-z0-9/._-]
    mode: transfer # transfer / merge / actions
    display:
      # material / skull(hash/base64/url)
      material: glass
      name: "&aCustom Group"
    items:
      - COPPER_INGOT
    # Clasificación de artículos
    # B = tablero de fondo
    # b = devolver
    # R = Búsqueda en tiempo real (si la función no está activada, se mostrará en el panel de fondo)）
    # C = Lista de colecciones (se muestra en el panel de fondo si la función no está activada)）
    # c = Elementos recopilados (la función se mostrará en el panel de fondo si la función no está activada)）
    # S = buscar
    # i = cosa
    # P = Pagina anterior
    # N = Página siguiente
    formats:
      - "BbBBBBRSB"
      - "iiiiiiiii"
      - "iiiiiiiii"
      - "iiiiiiiii"
      - "iiiiiiiii"
      - "BPBcCBBNB"
  group2:
    enabled: false
    tier: 2
    id: custom-group2
    mode: merge
    display:
      material: grass
      name: "&aCustom Group2"
    groups:
      - slimefun:misc
    items:
      - ZINC_INGOT
    formats:
      - "BbBBBBRSB"
      - "iiiiiiiii"
      - "iiiiiiiii"
      - "iiiiiiiii"
      - "iiiiiiiii"
      - "BPBcCBBNB"
  group3:
    enabled: false
    tier: 3
    id: custom-group3
    mode: merge
    actions:
      # %player% = player name
      - "command /say 1" # "command <command>" # run as console
      - "commandp /say 1" # "commandp <command>" # run as player
      - "link https://www.bilibili.com/video/BV1GJ411x7h7" # "link <link>" # open link for the player
      - "sayp 101" # "sayp <message>" # chat the message
      - "lookupitem TIN_CAN" # "lookupitem <id>" # lookup item
      - "lookupgroup slimefun:misc" # "lookupgroup <group_id>" # lookup group
